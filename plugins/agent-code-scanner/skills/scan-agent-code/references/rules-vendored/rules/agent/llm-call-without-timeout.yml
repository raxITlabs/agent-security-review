id: scope.llm-call-without-timeout
language: python
severity: warning
message: |-
  LLM API call has no explicit timeout. Denial-of-wallet risk from runaway requests.
  Architectural fix: set per-request timeout aligned with cost budget (Shift 1).
metadata:
  shift: scope
  component: agent
  maestro_layer: L1   # Foundation Models — DoS via computational exhaustion
  framework: maestro-l1
  references:
    - https://cloudsecurityalliance.org/blog/2025/02/06/agentic-ai-threat-modeling-framework-maestro
    - https://arxiv.org/abs/2508.10043
rule:
  all:
    - any:
        - pattern: $CLIENT.chat.completions.create($$$)
        - pattern: $CLIENT.messages.create($$$)
        - pattern: $CLIENT.complete($$$)
        - pattern: bedrock.invoke_model($$$)
    - not:
        has:
          stopBy: end
          any:
            - pattern: timeout=$$$
            - pattern: request_timeout=$$$
            - pattern: max_retries=$$$
