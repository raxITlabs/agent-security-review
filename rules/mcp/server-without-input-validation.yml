id: scope.mcp-server-without-input-validation
language: python
severity: error
message: |-
  MCP server tool reaches a dangerous sink (shell / SQL / exec) with its parameters unvalidated. Tool poisoning / RCE risk.
  Architectural fix: validate every tool parameter against an allowlist before any side effect (Shift 1).
metadata:
  shift: scope
  component: mcp
  confidence: high
  framework: msrc-prompts-become-shells
  references:
    - https://www.microsoft.com/en-us/security/blog/2026/05/07/prompts-become-shells-rce-vulnerabilities-ai-agent-frameworks/
    - https://modelcontextprotocol.io/specification/draft/basic/security_best_practices
# Anchor on decorated_definition (kind first inside all) so the FastMCP
# call-decorator `@mcp.tool()` (and `@server.tool` / `@app.tool`, with or without
# parens) is actually matched — the old `kind: function_definition` anchor matched
# no decorated function.
rule:
  all:
    - kind: decorated_definition
    - has:
        kind: decorator
        regex: "(server|mcp|app|tool_router|fastmcp)\\.tool"
    - has:
        stopBy: end
        any:
          - pattern: subprocess.$M($$$)
          - pattern: os.system($$$)
          - pattern: os.popen($$$)
          - pattern: eval($$$)
          - pattern: exec($$$)
          - pattern: $CUR.execute(f"$$$")
