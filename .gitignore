.DS_Store
*.pyc
__pycache__/
.venv/
fixtures/_cloned/
*.tmp
*.log
manifest.json

# Skill-creator dev artifacts (eval fixtures with planted vulns + run workspace) — not shipped
skill-dev/
# Skill runtime rule cache (fetched from GitHub on demand)
skills/agent-security-review/.cache/
findings.json

# local eval/audit artifacts (contain sample secret tokens used as rule fixtures)
agent-security-review-workspace/
